{#- Encart « Méthode et limites » (US-202, ADR-002 — EU AI Act art. 50).
    Inséré dans CHAQUE variante, immédiatement après la couverture (page 2 des
    variantes multi-pages ; bloc compact sur le one-pager). Contenu trilingue
    sélectionné par context.lang ; HTML brut (markdown-it le laisse passer).
    Le dir RTL est posé globalement par renderer.py sur <html>, et répété ici
    pour que le Markdown standalone (render_md) reste correct en arabe. -#}
{%- set _ml = {
  "fr": {
    "title": "Méthode et limites",
    "method": "Ce rapport est produit par simulation multi-agents : des personas générées par intelligence artificielle réagissent au scénario étudié dans des arènes simulées (réseaux sociaux simulés et, le cas échéant, une arène de convictions simulée). Les contenus, citations et dynamiques présentés sont synthétiques.",
    "limits": "Cette simulation est un stress-test de décision : elle explore des réactions possibles, n’annonce pas ce qui arrivera, ne remplace pas une étude terrain, et ses résultats varient selon le modèle d’IA et le paramétrage utilisés. Lorsque la simulation est ancrée sur des données réelles, le rapport le précise explicitement.",
    "marking": "Contenu généré par IA — marqué comme synthétique dans les métadonnées de ce document."
  },
  "en": {
    "title": "Method and limits",
    "method": "This report is produced by multi-agent simulation: AI-generated personas react to the scenario under study in simulated arenas (simulated social networks and, where applicable, a simulated conviction arena). The content, quotes and dynamics presented are synthetic.",
    "limits": "This simulation is a decision stress-test: it explores possible reactions, does not announce what will happen, does not replace field research, and its results vary with the AI model and configuration used. When the simulation is grounded on real data, the report states it explicitly.",
    "marking": "AI-generated content — marked as synthetic in this document’s metadata."
  },
  "ar": {
    "title": "المنهجية والحدود",
    "method": "أُنتج هذا التقرير عبر محاكاة متعددة الوكلاء: شخصيات مولَّدة بالذكاء الاصطناعي تتفاعل مع السيناريو المدروس داخل ساحات محاكاة (شبكات اجتماعية مُحاكاة، وساحة قناعات مُحاكاة عند الاقتضاء). المحتويات والاقتباسات والديناميكيات المعروضة اصطناعية.",
    "limits": "هذه المحاكاة اختبارُ تحمّلٍ للقرار: فهي تستكشف ردود فعل ممكنة، ولا تُعلن ما سيحدث، ولا تحلّ محلّ الدراسة الميدانية، وتختلف نتائجها باختلاف نموذج الذكاء الاصطناعي والإعدادات المستخدمة. وعندما تُرسى المحاكاة على بيانات حقيقية، يُبيّن التقرير ذلك صراحةً.",
    "marking": "محتوى مولَّد بالذكاء الاصطناعي — مُعلَّم كمحتوى اصطناعي في البيانات الوصفية لهذا المستند."
  }
} -%}
{%- set _t = _ml.get(context.lang, _ml["fr"]) -%}
<div class="method-limits"{% if context.lang == "ar" %} dir="rtl"{% endif %}>

**{{ _t.title }}**

{{ _t.method }}

{{ _t.limits }}

<span class="method-limits-marking">{{ _t.marking }}</span>

</div>
