{% from '_macros.md.j2' import kpi_card, callout %}
# {{ context.title | normalize(context.lang) }}

**Framework d'analyse :** {{ context.framework | upper }}{% if context.package %} — **Package :** {{ context.package }}{% endif %}

**Date de génération :** {{ generated_at | format_date(lang=context.lang) }}

---

*Propulsé par **Bassira** — une solution AIMPOWER*

---

{% if context.kpi_hero %}
## KPI Héro

{# Le label de la KPI Héro est intentionnellement court : on évite de réinjecter
   le verdict complet (souvent une phrase longue qui rendait illisible la carte). #}
{{ kpi_card(
    label='Confiance globale',
    value=(context.kpi_hero.confidence_pct | round(1) | string) + ' %',
    unit='de conviction agrégée'
) }}

| Indicateur | Valeur |
| --- | --- |
| Niveau d'alignement | {{ context.kpi_hero.confidence_pct | round(1) }} % |
| Indice de fiabilité | {{ context.kpi_hero.brier | reliability_pct }} % |
{% for camp, pct in context.kpi_hero.scenario_distribution.items() %}
| {{ camp | position_label }} | {{ pct | round(1) }} % |
{% endfor %}

{% if context.kpi_hero.verdict %}

**Verdict synthétique :** {{ context.kpi_hero.verdict | normalize(context.lang) }}
{% endif %}

{% endif %}

---

> **Avertissement :** Ce rapport est produit par un système de simulation multi-agents. Les résultats sont de nature probabiliste et ne constituent pas des conseils d'investissement, juridiques ou stratégiques. Toute décision doit faire l'objet d'une validation par des experts humains qualifiés.

---
